{
  "name": "api",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
  "start": "json-server --watch db.json --port 10000 --host 0.0.0.0 --static ./public"
}
,
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
